<UserControl
    x:Class="WinKVM.Views.LoginPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid HorizontalAlignment="Center" VerticalAlignment="Center" Width="360">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Logo / title -->
        <StackPanel Grid.Row="0" HorizontalAlignment="Center" Spacing="8" Margin="0,0,0,24">
            <FontIcon Glyph="&#xE7F4;" FontSize="48"/>
            <TextBlock Text="WinKVM" Style="{StaticResource TitleTextBlockStyle}" HorizontalAlignment="Center"/>
            <TextBlock Text="Raritan KX III/IV Client" Style="{StaticResource CaptionTextBlockStyle}"
                       HorizontalAlignment="Center" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"/>
        </StackPanel>

        <!-- Saved profiles -->
        <ComboBox x:Name="ProfileCombo" Grid.Row="1" HorizontalAlignment="Stretch"
                  Header="Saved Profiles" Margin="0,0,0,8"
                  SelectionChanged="ProfileCombo_SelectionChanged"/>

        <!-- Host -->
        <TextBox x:Name="HostBox" Grid.Row="2" Header="Host / IP" PlaceholderText="192.168.1.1"
                 Margin="0,0,0,8"/>

        <!-- Port -->
        <NumberBox x:Name="PortBox" Grid.Row="3" Header="Port" Value="443"
                   Minimum="1" Maximum="65535" Margin="0,0,0,8"
                   SpinButtonPlacementMode="Inline"/>

        <!-- Username -->
        <TextBox x:Name="UsernameBox" Grid.Row="4" Header="Username" Margin="0,0,0,8"/>

        <!-- Password -->
        <PasswordBox x:Name="PasswordBox" Grid.Row="5" Header="Password" Margin="0,0,0,8"/>

        <!-- Save profile -->
        <CheckBox x:Name="SaveProfileCheck" Grid.Row="6" Content="Save profile" IsChecked="True" Margin="0,0,0,16"/>

        <!-- Connect button -->
        <Button x:Name="ConnectBtn" Grid.Row="7" Content="Connect" HorizontalAlignment="Stretch"
                Style="{StaticResource AccentButtonStyle}" Click="ConnectBtn_Click"/>
    </Grid>
</UserControl>
